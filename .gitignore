# ───────────────────────────────────────────────────────────
# 【Git管理対象外ファイルリスト】
# ───────────────────────────────────────────────────────────

# ── Python 環境・キャッシュ ──
# 仮想環境フォルダ、PyInstallerやビルド時に生成されるキャッシュ
__pycache__/
*.pyc
venv/
.venv/
env/
dist/
build/
*.spec
*.exe
test_output.txt

# ── 成果物フォルダ ──
# 実行時に生成されるFLACログファイルを含むルートフォルダ全体を除外。
Aivis_AudioLog/

# ── 個人の設定・機密情報 ──
# ユーザー固有の設定値（話者ID, ローカルパス, アーティスト名等）を含むため除外。
config.local.json

# 各ユーザーがローカルで用意するアートワークファイルのため除外。
assets/cover.jpg

# ── OSのシステムファイル ──
.DS_Store
Thumbs.db
